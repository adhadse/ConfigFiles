import '../style/index.css';
import { JupyterFrontEndPlugin } from '@jupyterlab/application';
import { IThemeManager } from '@jupyterlab/apputils';
import { TranslationBundle } from '@jupyterlab/translation';
import { LabIcon } from '@jupyterlab/ui-components';
import { CompletionItemKindStrings, ICompletionIconSet, ICompletionTheme, ILSPCompletionThemeManager } from './types';
export declare class CompletionThemeManager implements ILSPCompletionThemeManager {
    protected themeManager: IThemeManager;
    protected current_icons: Map<string, LabIcon>;
    protected themes: Map<string, ICompletionTheme>;
    private current_theme_id;
    private icons_cache;
    private icon_overrides;
    private trans;
    constructor(themeManager: IThemeManager, trans: TranslationBundle);
    protected is_theme_light(): boolean;
    get_iconset(theme: ICompletionTheme): Map<keyof ICompletionIconSet, LabIcon>;
    protected update_icons_set(): void;
    get_icon(type: string): LabIcon | null;
    protected get current_theme_class(): string;
    set_theme(id: string | null): void;
    protected get current_theme(): ICompletionTheme | null;
    register_theme(theme: ICompletionTheme): void;
    /**
     * Display the registered themes in a dialog,
     * both for the user to know what they can choose from,
     * and for the developer to quickly check how the icons
     * from each theme would look rendered.
     */
    display_themes(): void;
    set_icons_overrides(iconOverrides: Record<string, CompletionItemKindStrings>): void;
}
export declare const COMPLETION_THEME_MANAGER: JupyterFrontEndPlugin<ILSPCompletionThemeManager>;
